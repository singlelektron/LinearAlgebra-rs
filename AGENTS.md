# 开发约定

## 项目目标与当前阶段

LinearAlgebra-rs 是使用 Rust 开发的线性代数表达式工作台，提供 CLI 与 TUI，共享变量、表达式和计算能力，支持浮点、有理数和有限范围的符号计算。

当前已提供首个可运行的表达式工作台；具体功能、使用方法与限制以 README 为准。不得将设计示例、路线图或未执行的检查描述为已实现、已通过。

## 分支、提交与 PR

1. 开始任务前检查工作区、当前分支和远端状态，保留用户已有修改；不得覆盖、丢弃或夹带无关变更。
2. 获取远端更新，从最新的 `origin/main` 新建 `feat/<topic>`、`fix/<topic>` 或 `docs/<topic>` 分支。工作区已有修改时先确认归属，避免切换分支破坏工作。
3. 不得直接在 `main` 开发。初始化默认分支的空引导提交是唯一例外，后续实际内容都必须通过 PR。
4. 按完整逻辑单元提交，使用英文 Conventional Commits，例如 `feat: add matrix multiplication`。提交应便于独立理解和审查，不机械地逐行或逐文件拆分。
5. 完成与变更相关的验证后 push 分支，并创建面向 `main` 的 PR。PR 说明具体问题、最终变更、实际验证结果及已知限制。
6. 交付 PR 链接并保持未合并；未经用户明确授权，不得合并或启用自动合并。

不提交密钥、凭据、构建产物或无关文件，不使用破坏性 Git 操作清理用户工作。

## 架构与计算语义

- 计算核心、表达式解析及会话状态、CLI 和 TUI 分离；界面复用共享逻辑，不各自实现数学算法或表达式语义。
- CLI 支持单次表达式与持续交互；TUI 支持表达式输入、变量浏览、结果与步骤查看。语法和符号能力边界应在 README 和功能 PR 中明确。
- 显式区分浮点近似值、有理数精确值和符号结果；不得静默将精确计算降级为浮点计算。混合模式的转换规则必须明确说明并验证。
- 对维度不匹配、非法输入、奇异矩阵及无法完成的运算提供可理解的错误。浮点算法应说明容差与数值限制，不将近似结果宣称为精确结果。
- 符号运算保留适用条件与必要假设，例如参数非零条件；不能确定的结论不得当作无条件成立。
- 精确消元步骤应与实际运算一致；具体库、表达式语法和符号能力边界在实现前由相应 PR 明确。

## 文档与验证

- README 使用英文。实现或修改用户可见功能时同步更新 README，并区分已实现能力与规划能力。
- 文档变更检查 Markdown 结构、相对链接目标、示例准确性、提交范围及 `git diff --check`。
- 建立 Rust 工程后，提交前执行 `cargo fmt --all -- --check`、`cargo clippy --workspace --all-targets --all-features -- -D warnings` 和 `cargo test --workspace --all-features`。若检查受环境限制，明确记录未完成项和原因，不宣称通过。
- 测试覆盖行为及数学契约：正常计算、维度边界、错误路径，以及对应模式的精确性或容差；按新增能力补充回归测试。
- 用户已将实现范围扩展为完整 CLI/TUI 计算器；按计算核心、表达式会话和终端界面划分逻辑提交。后续开发仍应采用边界明确、可验证的增量任务。
